import { SrPermissionsDirective } from './sr-permissions.directive';

describe('SrPermissionsDirective', () => {
  it('should create an instance', () => {
    const directive = new SrPermissionsDirective();
    expect(directive).toBeTruthy();
  });
});
